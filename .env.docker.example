# ==========================================
# Docker Production Environment Variables
# ==========================================
# Copy this file to .env and configure for your environment
# Usage: cp .env.docker.example .env
# ==========================================

# ==========================================
# Database Configuration
# ==========================================
# Option 1: Local PostgreSQL (docker-compose.prod.yml)
# DB_USER=trackinguser
# DB_PASSWORD=your-secure-password-here
# DB_NAME=tracking_system

# Option 2: Supabase (docker-compose.supabase.yml)
# Connection Pooler (for application - port 6543)
DATABASE_URL="postgresql://postgres.YOUR_PROJECT_ID:YOUR_PASSWORD@aws-1-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
# Direct Connection (for migrations - port 5432)
DIRECT_URL="postgresql://postgres.YOUR_PROJECT_ID:YOUR_PASSWORD@aws-1-ap-south-1.pooler.supabase.com:5432/postgres"

# ==========================================
# Backend Configuration
# ==========================================
# JWT Secret - MUST change in production!
JWT_SECRET=your-super-secret-jwt-key-change-this

# CORS - Allowed origins (comma separated)
ALLOWED_ORIGINS=http://localhost,https://yourdomain.com

# Frontend URL for tracking links in notifications
FRONTEND_URL=http://localhost

# ==========================================
# Frontend Configuration
# ==========================================
# Frontend port (default: 80)
FRONTEND_PORT=80

# API URL (relative or absolute)
# Use relative /api/v1 if nginx is proxying
# Use absolute http://backend:5000/api/v1 for direct access
VITE_API_URL=/api/v1

# App name displayed in UI
VITE_APP_NAME=PakkuNeko

# ==========================================
# LINE OA Integration (Optional)
# ==========================================
LINE_CHANNEL_SECRET=your-line-channel-secret
LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token

# ==========================================
# pgAdmin (Optional - Database Admin)
# ==========================================
PGADMIN_PORT=5050
PGADMIN_EMAIL=admin@pakkuneko.local
PGADMIN_PASSWORD=your-pgadmin-password

# ==========================================
# Production Tips
# ==========================================
# 1. Never commit .env file to git
# 2. Use strong, unique passwords
# 3. Use HTTPS in production
# 4. Consider using Docker secrets for sensitive data
# 5. Backup postgres_data volume regularly
